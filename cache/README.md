# Cache

This directory is reserved for runtime cache documentation. The application cache is stored outside the Git repository by default and is keyed by SHA-256 document content hashes, so renaming the same PDF does not create a second indexed copy.